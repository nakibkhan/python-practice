textFile = 'grid.txt'

def prod_backslash(num_list, index):
    return 1

def prod_frontslash(result_list, index)
    

with open(textFile) as fp:
    product_list = []
    file_nums = []
    for line in fp:
        file_nums.append(line.strip('\n').split(" "))

